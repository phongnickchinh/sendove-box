; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-c3-devkitm-1]
platform = espressif32
board = esp32-c3-devkitm-1
framework = arduino
board_build.partitions = partitions_ota.csv

monitor_speed = 115200

; --- Thư viện bên thứ 3 ---
lib_deps =
    lovyan03/LovyanGFX@^1.1.12
    bitbank2/JPEGDEC
    bblanchon/ArduinoJson@^7.0.0

; --- Build flags ---
; LovyanGFX: Cấu hình trong code (DisplayDriver), không cần defines ở đây
; USB CDC: Serial qua USB nội bộ ESP32-C3 (GPIO 18/19)
build_flags =
    -I include
    -D ARDUINO_USB_CDC_ON_BOOT=1
    -D ARDUINO_USB_MODE=1

; ============================================================================
; Wokwi Simulation Environment (tạm giữ cho tương lai)
; ============================================================================
; [env:wokwi]
; platform = espressif32
; board = esp32-c3-devkitm-1
; framework = arduino
; monitor_speed = 115200
; upload_protocol = custom
; upload_command = echo "Wokwi env: use Wokwi simulator, not hardware upload"
; lib_deps =
;     lovyan03/LovyanGFX@^1.1.12
;     bitbank2/JPEGDEC
;     bblanchon/ArduinoJson@^7.0.0
; build_flags =
;     -I include
;     -D ARDUINO_USB_CDC_ON_BOOT=1
;     -D ARDUINO_USB_MODE=1
;     -D WOKWI_SIMULATION=1
